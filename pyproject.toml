[project]
name = "jobs_hk"
version = "1.0"
dependencies = [
    "requests",
    "bs4",
    "sqlalchemy",
    "sqlmodel"
]

[project.scripts]
run-search-job = "jobs_hk.main:search"
run-fill-job = "jobs_hk.main:fill"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"